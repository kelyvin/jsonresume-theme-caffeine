{{#resume.basics}}
    {{#if summary}}
        <div class="container summary-container">
            {{> title value="Objective"}}
            <p class="summary">
                {{summary}}
            </p>
        </div>
    {{/if}}
{{/resume.basics}}